import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#BC2DAE',
      light: '#CD9B9E',
      dark: '#371935',
      contrastText: '#ffffff',
    },
  },
});

export default theme;